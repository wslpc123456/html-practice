复习了之前学习的方法
typeof判断类型(不加括号)
==不会判断数据类型 ===会判断
push(1,2)在数组尾部加元素 pop(没有值)移除数组尾部一个元素
shift()从头部移除一个元素 unshift(1,2)从头部插入多个元素
splice(1,2,3,4)表示从arr[1]开始 移除2个元素(包括arr[1]),将3,4插入原来的位置 会改变原数组
slice(1,2)表示截取从arr[1]到arr[2]的元素
join('')显示数组时在元素之间添加''之间的内容(例如+)
sort 从小到大排列数组 reverse 从大到小排列数组
for in遍历对象枚举属性的名字 object[key]=value
.construtor 取得创建对象函数
.hasOwnProperty 检测给定属性在对象中是否存在
.prototype 原型对象
根据属性不重复的特性去掉数组的重复项(但是会自动排序)
arguments只能在函数内调用,且要赋值
this始终指向执行者 f执行时this指向window window.k=20相当于把k变为了全局变量赋值20
(function(){})()块级作用域 函数直接执行 外部无法访问内部变量
鸭式辨形法
1.判断接口的参数数量
2.判断是否正确传输了方法名称
3.实例化一个接口
4.实例化一个类
检验接口里的方法
1.至少得有两个参数(实例对象名 实现的接口名)
2.判断方法是否是接口类的类型
3.遍历接口名中的每一个方法查看是否有对应的值