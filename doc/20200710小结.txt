简单的函数链式调用
dog.run().eat().sleep()
使用return this来实现(核心)
模拟jquery底层代码
(function(){})()使用了块级作用域 程序启动时里面的代码直接执行
内部的成员变量外部无法访问(除了不加 var的变量)
$最常用的对象,返回给外界 大型程序开发一般使用'_'作为私有对象(规范)
正则表达匹配id选择器 /#\w+//表示接收到字符串后第一位为#号 #号后面的字符为任意字母数字则返回true
this.dom; 用此属性接收所得到的元素 如果匹配成功,则接收dom元素 arguments[0]='#inp'
在Function类上扩展一个可以实现链式编程的方法(使用return this)
在_$的原型对象上加一些公共的方法 给你得到的元素注册事件
window先注册一个全局变量 从而与外界产生关系
(function(){})(window)程序入口把window传入作用域中
实例对象构建完成后做两件事1.判断接口的参数类型 2.检验接口里的方法
1.判断接口的参数类型 2.接收methods数组内的方法名 加入到内部定义的一个空数组中 3.检查元素是否为string类型
检验接口的静态方法 1:判断函数是否为接口类 2.遍历接口里的方法名 用object.[key]查询是否有对应value